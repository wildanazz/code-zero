version: '3.9'
services:
  client:
    build:
      context: ./client
      dockerfile: Dockerfile.dev
    ports:
      - '3000:3000'
    volumes:
      - ./client:/home/node/app
      - /home/node/app/node_modules